# Moltbook — PowerCoreAi's API key (from https://www.moltbook.com after registration)
# Required to see live agent data and execute approved social actions.
MOLTBOOK_API_KEY=moltbook_xxx

# Maltbot base URL (for Moltbook skill wiring — propose API URL)
# MALTBOT_BASE_URL=http://localhost:3000

# OpenClaw (optional)
# OPENCLAW_CLI_PATH=openclaw
# OPENCLAW_AGENT_ID=main
# OPENCLAW_AGENT_TIMEOUT_MS=120000
# OPENCLAW_GATEWAY_URL=http://127.0.0.1:18789
# OPENCLAW_GATEWAY_PORT=18789
# OPENCLAW_GATEWAY_TOKEN=

# Command Center — multi-model consensus (Society of Minds)
# At least one key is required for consensus; others are optional (graceful degradation).
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
# GOOGLE_API_KEY=...
# XAI_API_KEY=...

# Command Center — optional (voice, fleet, notifications)
# ELEVENLABS_API_KEY=...
# TWILIO_ACCOUNT_SID=...
# TWILIO_AUTH_TOKEN=...
# TWILIO_PHONE_NUMBER=...
# DIGITALOCEAN_TOKEN=...
# RESEND_API_KEY=...
# NOTIFICATION_EMAIL=you@example.com

# Security layer — credential vault (optional)
# Set to enable storing API keys in the encrypted vault instead of env.
# Must be at least 16 characters. Agent never sees raw keys.
# VAULT_MASTER_PASSWORD=

# Platform sign-in (OAuth)
# Required for dashboard/onboarding; create a Google OAuth 2.0 Client (Web) in GCP Console.
# Redirect URI: {origin}/api/auth/callback/google
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Dev bypass: sign in without Google (optional). Set AUTH_DEV_BYPASS_SECRET and NEXT_PUBLIC_DEV_BYPASS=true
# AUTH_DEV_BYPASS_SECRET=dev
# NEXT_PUBLIC_DEV_BYPASS=true

# Development: reset onboarding on every dev server restart (optional)
# Set to true to clear active pair and onboarding draft so onboarding starts from step 1 each run.
# DEV_RESET_ONBOARDING=true
# NEXT_PUBLIC_DEV_RESET_ONBOARDING=true
